# Development toolchain for dusk.
# Install with: task setup  (or: brew bundle)

brew "go"            # Go compiler and toolchain
brew "go-task"       # task runner (Taskfile.yml)
brew "gofumpt"       # stricter gofmt (fmt tasks)
brew "golangci-lint" # meta linter (lint tasks)
